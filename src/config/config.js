const routes = {
    home: '/',
    following: '/following',
    profile: '@:nickname',
    upload: '/upload',
    search: '/search',
    live: '/live',
};

const config = {
    routes,
};

export default config;
